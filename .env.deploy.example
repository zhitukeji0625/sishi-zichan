# 复制为 .env 后由 docker compose 加载（勿提交真实 .env）
# 容器映射到宿主机的端口（宝塔反代 18084 -> 127.0.0.1:19084 时用 19084）
APP_PORT=19084
SESSION_SECRET=请替换为至少32位随机字符串
THIRD_PARTY_JWT_SECRET=请替换为至少32位随机字符串
# 用户浏览器实际地址（经 Apache 对外端口访问时写 :18084）
NEXT_PUBLIC_APP_URL=http://你的服务器IP或域名:18084
