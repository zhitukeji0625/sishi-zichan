# 复制为 `.env.test` 或写入根目录 `.env`，用于 `auction.test.ts` 集成测试（需可连上的 MySQL/MariaDB）
DATABASE_URL="mysql://root:root@127.0.0.1:3306/sishi"
SESSION_SECRET="test-session-secret-not-for-production"
THIRD_PARTY_JWT_SECRET="test-third-party-secret-not-for-production"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
COOKIE_SECURE="false"
