# 复制为 .env.test 后运行集成测试（需本机 MySQL，见 docker-compose）
# cp .env.test.example .env.test && docker compose up -d mysql && npx prisma db push
DATABASE_URL="mysql://root:root@127.0.0.1:3307/sishi"
SESSION_SECRET="test-session-secret-min-32-chars-long!!"
THIRD_PARTY_JWT_SECRET="test-third-party-jwt-secret"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
COOKIE_SECURE="false"
