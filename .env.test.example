# 集成测试：复制本文件为 .env.test（已被 .gitignore），再启动数据库：
#   docker compose -f docker-compose.yml -f docker-compose.test.yml up -d mysql
#   npx prisma db push
# 然后 npm run test 会加载 .env.test 并跑 placeBid 集成用例。
DATABASE_URL="mysql://root:root@127.0.0.1:3307/sishi"
SESSION_SECRET="test-session-secret-at-least-32-chars-long"
THIRD_PARTY_JWT_SECRET="test-third-party-jwt-secret-hmac"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
COOKIE_SECURE="false"
